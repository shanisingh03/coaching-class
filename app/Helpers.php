<?php

if (!function_exists('get_course_image')) {
    /**
     * Returns a complete URI of Image
     *
     * @param integer $course_id
     * to Course Details
     *  
     * @return string a string in Complete Path
     *
     * */
    function get_course_image($course_id)
    {
        
    }
}